node_modules/
dist/
.astro/
.wrangler/
test-results/
playwright-report/
tests/visual/**/*-actual.png
tests/visual/**/*-diff.png

# Local secrets. Globs so variants like .env.local and .dev.vars.production
# are caught too; the committed examples are the only exceptions.
.env*
!.env.example
.dev.vars*
!.dev.vars.example
